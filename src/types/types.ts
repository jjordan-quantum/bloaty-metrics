export enum CacheInterval {
  oneMin=60*1000,
  twoMin=2*60*1000,
  fiveMin=5*60*1000,
  tenMin=10*60*1000,
  fifteenMin=15*60*1000,
  twentyMin=20*60*1000,
  thirtyMin=30*60*1000,
  oneHour=60*60*1000,
}
